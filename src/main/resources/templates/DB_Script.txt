-- Table: public.password_verify_token

-- DROP TABLE IF EXISTS public.password_verify_token;

CREATE TABLE IF NOT EXISTS public.password_verify_token
(
    email character varying(255) COLLATE pg_catalog."default" NOT NULL,
    token character varying(255) COLLATE pg_catalog."default" NOT NULL,
    expiry timestamp without time zone NOT NULL,
    id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 9223372036854775807 CACHE 1 )
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.password_verify_token
    OWNER to postgres;

================================

-- Table: public.uvm001

-- DROP TABLE IF EXISTS public.uvm001;

CREATE TABLE IF NOT EXISTS public.uvm001
(
    syskey bigint NOT NULL DEFAULT nextval('uvm001_syskey_seq'::regclass),
    autokey bigint,
    createddate character varying(255) COLLATE pg_catalog."default",
    modifieddate character varying(255) COLLATE pg_catalog."default",
    status bigint,
    username character varying(255) COLLATE pg_catalog."default",
    email character varying(255) COLLATE pg_catalog."default",
    password character varying(255) COLLATE pg_catalog."default",
    phone character varying(255) COLLATE pg_catalog."default",
    parentid bigint,
    n1 bigint,
    n2 bigint,
    n3 bigint,
    n4 bigint,
    n5 bigint,
    CONSTRAINT uvm001_pkey PRIMARY KEY (syskey)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.uvm001
    OWNER to postgres;